[filesystem]
data = /data
logs = /data/logs
media = /public/media
static = /static

[site]
; never run debug in production. Make sure you serve all requests for the
; `/static/` and `/media/` paths when debug is False.
; See [installation](https://docs.pretalx.org/administrator/installation/#step-7-ssl) for more information
debug = False
media = https://ref11devmedia.zrok.lcas.group:443/
url = https://ref11dev.zrok.lcas.group

[database]
backend = postgresql
name = pretalx
user = pretalx
password = veryunsecureplschange
host = db
port = 5432

[mail]
from = mhanheide@lincoln.ac.uk
host = mail
port = 25
#user = admin
#password = something
tls = False
ssl = False

[redis]
location = redis://redis/0
sessions=true

[celery]
backend = redis://redis/1
broker = redis://redis/2

; Registers the pretalx_ref_wording plugin's locale as a full interface
; language (not just a content locale). Without this section, "en-rex" is
; only selectable as a content locale, never as the interface/UI language.
[language:en-rex]
name = English (REF Outputs)

